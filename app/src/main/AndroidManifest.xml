<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sa.demo.exoxycarouseldemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".DemoApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".views.preload.ImagePreloadActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".views.movie.MovieListActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".views.carousel.CarouselDemoActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".views.video.VideoListActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>